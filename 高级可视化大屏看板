<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>协作引力模型 - 高级数字看板</title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        :root { --bg: #0f172a; --card: #1e293b; --accent: #38bdf8; --text: #f1f5f9; }
        body { font-family: 'Inter', sans-serif; background: var(--bg); color: var(--text); margin: 0; padding: 20px; }
        .grid-container { display: grid; grid-template-columns: repeat(4, 1fr); gap: 20px; max-width: 1400px; margin: 0 auto; }
        .header { grid-column: span 4; display: flex; justify-content: space-between; align-items: center; border-bottom: 1px solid #334155; padding-bottom: 20px; }
        .card { background: var(--card); padding: 25px; border-radius: 16px; box-shadow: 0 4px 6px rgba(0,0,0,0.3); }
        .stat-card { text-align: center; }
        .stat-card .label { font-size: 14px; color: #94a3b8; margin-bottom: 8px; }
        .stat-card .value { font-size: 36px; font-weight: 800; color: var(--accent); }
        .chart-card { grid-column: span 3; height: 500px; }
        .info-card { grid-column: span 1; display: flex; flex-direction: column; gap: 15px; }
        .network-sim { height: 200px; border: 1px dashed #334155; border-radius: 12px; display: flex; align-items: center; justify-content: center; position: relative; }
        .node { width: 40px; height: 40px; border-radius: 50%; background: var(--accent); position: absolute; }
    </style>
</head>
<body>

<div class="grid-container">
    <div class="header">
        <div>
            <h1 style="margin:0; font-size: 28px;">OpenDigger 协作引力动态大屏</h1>
            <p style="margin:5px 0 0; color: #94a3b8;">React vs Vue 演化动力学实测 (2023-2024)</p>
        </div>
        <div style="text-align: right;">
            <div id="date">更新时间: 2024.10.31</div>
            <div style="color: #2ecc71;">● 系统运行正常</div>
        </div>
    </div>

    <!-- 核心统计 -->
    <div class="card stat-card"><div class="label">当前协作引力 (F)</div><div class="value">49.52</div></div>
    <div class="card stat-card"><div class="label">年度引力增幅</div><div class="value">+108.3%</div></div>
    <div class="card stat-card"><div class="label">开发者重合度峰值</div><div class="value">8.5%</div></div>
    <div class="card stat-card"><div class="label">生态耦合风险</div><div class="value" style="color:#fbbf24">低</div></div>

    <!-- 主图表 -->
    <div class="card chart-card">
        <canvas id="mainChart"></canvas>
    </div>

    <!-- 拓扑模拟与关键点 -->
    <div class="card info-card">
        <h3>协作拓扑模拟</h3>
        <div class="network-sim" id="netSim">
            <div class="node" style="left: 20%; top: 40%; background: #61dafb;" title="React"></div>
            <div class="node" style="right: 20%; top: 40%; background: #42b883;" title="Vue"></div>
            <svg width="100%" height="100%" style="position:absolute; top:0; left:0;">
                <line x1="25%" y1="50%" x2="75%" y2="50%" style="stroke:rgba(56,189,248,0.5); stroke-width:8; stroke-dasharray: 5,5" />
            </svg>
            <div style="position:absolute; bottom:10px; font-size:12px;">引力牵引带宽: 强</div>
        </div>
        <div style="font-size: 14px; line-height: 1.6;">
            <strong>本月分析：</strong><br>
            React 指标维持高位，Vue 的参与者重合率稳步上升。引力模型显示两项目已进入“深度互补区”，建议关注跨社区工具链的开发机会。
        </div>
    </div>
</div>

<script>
    const ctx = document.getElementById('mainChart').getContext('2d');
    new Chart(ctx, {
        type: 'line',
        data: {
            labels: ["2023-01", "2023-05", "2023-12", "2024-01", "2024-05", "2024-10"],
            datasets: [{
                label: '协作引力指数 (Gravity Force)',
                data: [13.34, 31.42, 9.28, 23.58, 56.42, 49.52],
                borderColor: '#38bdf8',
                backgroundColor: 'rgba(56,189,248,0.1)',
                fill: true,
                tension: 0.4,
                pointRadius: 8,
                pointHoverRadius: 12
            }, {
                label: '引力趋势预测',
                data: [null, null, null, null, null, 49.52, 51.2, 53.5],
                borderColor: '#fbbf24',
                borderDash: [5, 5],
                fill: false
            }]
        },
        options: {
            responsive: true,
            maintainAspectRatio: false,
            plugins: {
                legend: { labels: { color: '#f1f5f9' } }
            },
            scales: {
                y: { grid: { color: '#334155' }, ticks: { color: '#94a3b8' } },
                x: { grid: { display: false }, ticks: { color: '#94a3b8' } }
            }
        }
    });
</script>
</body>
</html>
